- name: Storing output
  hosts: localhost
  tasks:
    - name: Print output
      ansible.builtin.shell: uptime
      Register: Output

    - name: Printing the output
      ansible.builtin.debug:
        msg: "{{output}}"